#include "monty.h"

/**
 * nop_func - The opcode nop doesn’t do anything.
 *
 * Description: This function doesn’t do anything :)
 *
 * @stack: Pointer to the pointer to the top of the stack.
 * @line_number: The line number where the "swap" operation appears
 * in the Monty file.
 */

void nop_func(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
